#include <bits/stdc++.h>
using namespace std;
#define ar array
#define ll long long
const int MAX_N = 1e5 + 1;
const ll MOD = 1e9 + 7;
const ll INF = 1e9;

int solve(stirng &v,ll k)
{
    unordered_map<int,bool> mp;
    
  for(int i = 0; i <k; i++)
  {
            unordered_map<int,>
  }  
}


int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
      ll t;
      cin>>t;
      while(t--)
       {ll n;
       cin>>n;
       string s;
       cin>>s;
       ll k;
       cin>>k;

    //    vector<string> v;
    //    for(int i=0;i<n;i++)
    //    {  string s;
    //        cin>>s;
    //        v.push_back(s);
    //    }
    //   
      cout<<solve(s);
       }
return 0;
}